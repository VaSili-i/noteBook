export const list = 0;

/*
import React from 'react';
import {modalWindow} from "../models/componentModels";

const GeneralModal = function({listTask: any}) {

    return(
    <div>

    </div>
    );
};

export default GeneralModal;
*/
